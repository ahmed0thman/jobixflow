# Source notes — Client follow-up meeting

**Date:** 2026-08-17, 20:53
**Participants:** the UI/UX designer and **the client** — the system owner who commissioned JobixFlow. Not Eng. Zeinab; this is a different meeting with a different speaker than the 2026-08-05 developer handover.
**Original location:** `recorded_meetings/2026-08-17 20_53/` — both `transcript.txt` (raw) and `transcript_corrected.txt` (cleaned) exist there. **This copy is the corrected version.**
**Citation tag:** `[T2:<line>]` — distinct from `[T:<line>]`, which is reserved for the 2026-08-05 developer transcript. Never mix the two tags.

## What this source is

This is explicitly the client meeting that `Q-19` (and several other open questions) said was scheduled to settle unresolved points. The designer works through his open-questions list directly with the client and gets concrete answers on: the meaning of "wallet," audit log scope vs. company data sovereignty, delete vs. archive, job priority, Twilio number ownership, payment gateways, refunds vs. disputes, expenses, and — newly — a full walkthrough of the job state machine and confirmation that invoicing is a real, technician-triggered feature.

## Authority relative to other sources

Per [Rule 00 · R00-2](../../../rules/00-working-agreement.md) the written client file outranks any meeting transcript, and that ordering is unchanged here. But **relative to the 2026-08-05 developer transcript, this source should generally be preferred on points of direct conflict** — it is the client speaking in their own voice about their own business, not the developer's secondhand account or invention (recall `[T:253]`, where the developer admits parts of the permission model are her own authoring, never reviewed by the client). Where this source repeats something the developer already said, treat it as corroboration. Where it contradicts the developer transcript, this source wins and the BRD should say so. Where it contradicts the **written** client file (e.g. the gateway names — see below), that is a genuine open contradiction between two things the client themselves said at different times, not an automatic override — log it, don't silently pick one.

## Reliability caveats

- Already run through a transcript-correction pass (this is the `_corrected` file), so word-level ASR errors should be fewer than the 2026-08-05 source. Still auto-transcribed colloquial Arabic with no speaker diarization — turn boundaries are inferred from content (short acknowledgements like "تمام", "نعم" are almost always the designer; long explanatory answers are the client).
- Several topics are explicitly deferred by the client to a **follow-up written message in the project group chat**, not covered in the meeting itself: the full list of job-cancellation/no-contact reasons (`[T2:102]`), dispute-evidence specifics via the payment provider's API docs (`[T2:182]`). Do not treat their absence here as a gap in the client's intent — it is a promised, not-yet-delivered artifact. Track it as an open question rather than inventing the content.
- Line 173 ("هتتبعت له بلانك صح") is a garbled fragment — likely "link" (i.e. the payment link), mis-transcribed. Not load-bearing on its own; treat as `[ASSUMPTION]` if ever cited.

## Section map

| Lines | Content |
|---|---|
| 5–17 | **Wallet defined** — two wallets (platform-as-payment-provider, per-company); no technician-to-technician transfers; weekly reset Sunday midnight; wallet is fundamentally a transaction log / source of truth, not a movable-money account. Resolves `Q-01`. |
| 18 | Stripe named as the gateway that "runs all these operations" for the platform-provider wallet |
| 25–29 | Org hierarchy naming: Platform Admin → "Platform Dispatch" (internally "First Line") → Company Admin → Company Dispatcher → Technician |
| 30–34 | **Audit log scope vs. company data sovereignty** — the platform's log only certifies operations conducted *through the platform's own infrastructure*; anything a company does entirely on its own payment gateway and its own Twilio number is excluded, out of respect for the company's independent "entity" (كيان). Refines `Q-06` and `AUD-001`. |
| 34–36 | Platform revenue from company subscriptions is currently **manual/external** (demo → deal → manual billing), not built into the system yet; may become an in-product subscription/membership feature once the client base is large enough. Partial answer to `Q-11`. |
| 36–54 | Weekly technician settlement: technician is a commissioned contractor, not an employee; system nets cash-collected vs. card-collected per technician at week end; a manual adjustment path exists on the technician's weekly report line items (to correct system-missed entries) *before* the report is finalized; settlement itself (technician → company, or company → technician) happens off-platform (in-person cash, Zelle, Cash App) and the platform only needs a confirmed/collected mark. |
| 47–54 | **Expenses defined** — job-tied operational costs only (e.g. a $30 key/key-code purchase), tagged "paid by me" (technician) or "paid by company," entered by the technician at job close-out; deducted from job total before commission math. Resolves `Q-16`. |
| 55–62 | Report filtering requirements: same-day / same-week / previous-week / custom date range, plus condition filters (customer, technician, dispatcher, payment method). Hard constraint restated: **no cross-company visibility, and a technician never sees another technician's data.** Feeds `Q-08`, reinforces `TEN-002`. |
| 63–76 | **Delete vs. archive resolved** — no hard delete of live records; the Jobs delete button should become an **archive** action gated by a minimum record age (proposed one year, adjustable from the dashboard); periodic (quarterly/annual) full export to Excel/CSV before any purge satisfies the US 2-year legal-retrieval requirement the client is aware of. A job that has entered the system must run its process to completion or receive an explicit cancel + reason — it is never simply deleted mid-flight. Resolves `Q-07`. |
| 77–101 | **The job state machine, walked step by step** — and confirmed **fixed and identical across every company**, not company-configurable (new rule, no prior ID). Call intake → dispatch assigns to nearest/suitable free technician → technician accepts/rejects (accept-click timestamp tracked) → technician calls customer to confirm details → technician starts the trip → technician manually confirms arrival, which is the single action that both advances the state and captures GPS lat/long (GPS is deliberately **not** trusted for automatic arrival detection — the confirm is manual, echoing `LOC-002` with more mechanism detail). |
| 102–124 | **Job cancellation / no-show reasons** (partial list; full list promised separately) — customer resolved it themselves, customer did not answer, wrong details/wrong phone number entered at intake. **Service Call Fee** introduced — a standalone charge covering a technician's wasted trip when a job doesn't complete after dispatch (new fee concept, not previously named in any BRD). **Job reassignment between technicians** — e.g. price-shopping scenarios — and how the Service Call Fee applies to the technician who made the trip but didn't close the job. |
| 125–137 | **24-hour callback routing refined** — routes to whichever technician *currently* holds the job (i.e. follows a reassignment, not the original assignee); after 24 hours, routes to the **company dispatcher** for company-relevant jobs or **back to the platform** for platform-sourced jobs — origin determines the post-24h destination, not a single fixed target. Refines `COM-004`/`COM-005`. |
| 138–142 | **Priority resolved** — binary Now / Scheduled only. No effect on price, category, or which technician receives the job; purely a scheduling flag set by dispatch. Resolves `Q-05`. |
| 143–146 | **Twilio number ownership refined** — a company's number can change at any time (not fixed to a year, as previously assumed), tied to whenever they choose to change subscription. Only call/message **logs** are retained regardless of number changes — no need to version records against a specific number. Resolves `Q-10`. |
| 147–157 | **Payment gateways** — no hard cap of exactly three; named verbally as **Stripe, PayPal, Authorize.net** — note this **conflicts with the written client file**, which names Stripe, **Square**, Authorize.net (`Q-03`, unresolved contradiction, not silently picked). Each company must obtain and hold its own API credentials directly with the gateway/Twilio provider (KYC/background-check liability reasons) — the platform only performs the connection, never re-sells or co-signs access. |
| 158–171 | **Refund vs. dispute/chargeback distinguished** — a refund is the company voluntarily returning money (full or partial) after an informal customer phone complaint, as a goodwill gesture, with no formal request workflow described; a dispute/chargeback originates at the customer's bank, arrives via the payment gateway's own dispute API, and gives the company a window (client estimates 30–60 days, unconfirmed) to submit evidence. Both must be tagged/linked to the job as distinct states. Partial answer to `Q-15`; reinforces `FIN-F-003`. |
| 172–182 | **Proof of service** — technicians keep physical paper logbooks, get the customer's signature, and photograph the signed page plus the customer's ID; these photos are uploaded to the platform as dispute evidence after job close. Further dispute-evidence detail promised separately via the payment provider's own API documentation. |
| 184–201 | **24-hour messaging mechanics** — the technician sends SMS from inside the app (same channel as the masked call), which reaches the customer as an ordinary SMS from the Twilio number on their native phone app; there is no separate customer-facing messaging surface. Call and SMS recordings live in Twilio itself — the platform stores/displays a **link**, not the raw file. Clarifies `Q-17`. |
| 202–211 | **Invoicing confirmed as a real, scoped feature** — the technician decides per job whether to send an invoice; the invoice is issued under the **company's** name only (no logo needed, no platform name or branding anywhere on it), because each company owns its own payment relationship. Resolves `Q-12`. |

## Out of scope within this source

None observed — unlike the 2026-08-05 source, this meeting stays entirely on business logic. No Blade/React/database tangent to filter out.
